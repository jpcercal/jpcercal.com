dist:
  options:
    removeComments: true
    collapseWhitespace: true
  files:
    - 
      expand: true
      cwd: "<%= dist_path %>/"
      src:
        - "**/*.html"
      dest: "<%= dist_path %>/"
