options:
  key: "<%= googleApiKey %>"
  url: "https://jpcercal.com/"
desktop:
  options:
    locale: "en_GB"
    strategy: "desktop"
    threshold: 85
    paths: "<%= paths %>"
mobile:
  options:
    locale: "en_GB"
    strategy: "mobile"
    threshold: 80
    paths: "<%= paths %>"
