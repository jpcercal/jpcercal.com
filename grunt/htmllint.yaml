dist:
  options:
    ignore:
    - /This document appears to be written in w+ but the “w+” start tag has “w+="w+"”.
      Consider using “w+="w+"” (or variant) instead./
  src:
  - "<%= dist_path %>/**/*.html"
  - "!<%= dist_path %>/search-template.html"
